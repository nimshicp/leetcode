/**
 * @param {number[]} nums
 * @return {number}
 */
var findGCD = function(nums) {
    let min = Math.min(...nums)
    let max = Math.max(...nums)
    let maxDiv=[]
    for(let i=1 ;i<=max;i++){
        if(min%i===0 && max%i===0){
            maxDiv.push(i)
        }
    }
    return Math.max(...maxDiv)
};
findGCD([7,5,6,8,3])
